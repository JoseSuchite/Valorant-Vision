This will be replaced later

* Run 'python generate.py' to generate the training data
* Then open the 'rcnn_training.ipynb' to actually train the model
* Also note: this is all currently reallly bad and just for testing

Explanation:
* training_image_generator.py is a class that generates the training data for the model.
* generate.py is a wrapper script that handles calling the image generator and everything
* rcnn_training.ipynb is the notebook for training the model. Currently a notebook so that it's easier to work with

